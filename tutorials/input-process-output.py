# 1. Inputs go in the parentheses (Arguments)
def greet_user(username):
    # Processing happens inside
    greeting = f"Hello, {username}!"
    # Output is sent back
    return greeting

# Using the function
message = greet_user("Alok")
print(message)